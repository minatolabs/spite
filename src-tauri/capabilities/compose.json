{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "compose",
  "description": "Compose windows may manage themselves (close after send/cancel); scoped to compose-* only so the main window's surface stays unchanged",
  "windows": ["compose-*"],
  "permissions": [
    "core:window:allow-close",
    "core:window:allow-destroy",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize"
  ]
}
